{
  "name": "@randomify/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@randomify/shared": "workspace:*",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260815.1",
    "@electric-sql/pglite": "^0.5.5",
    "@randomify/pipeline": "workspace:*",
    "vitest": "^4.1.10",
    "wrangler": "^4.123.0"
  }
}
